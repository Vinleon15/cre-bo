"""tokensave — экономия токенов без потери качества.

Принцип: экономим не на нужной информации, а на повторной обработке уже
известной. Три механизма, по убыванию безопасности:
  1. prompt caching — повторный префикс стоит 0.1x. Риска для качества нет.
  2. кэш файлов по sha256 — файл разбирается один раз на версию. Риска нет.
  3. сжатие истории — единственное место, где можно что-то потерять,
     поэтому цифры, даты и условия выносятся в пины дословно, а при любом
     сомнении контекст расширяется до полного.

    from tokensave import Saver
    saver = Saver(mode="normal", session="проект-1")
    answer = saver.ask("Какой срок оплаты?", files=["dogovor.pdf"])
    print(answer.text, answer.saved_pct)
"""

from .client import Answer, Saver

__all__ = ["Saver", "Answer"]
__version__ = "0.1.0"
